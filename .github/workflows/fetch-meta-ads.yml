name: Fetch Meta Ads Data

on:
  schedule:
    - cron: '0 */6 * * *'   # a cada 6 horas
  workflow_dispatch:          # permite rodar manualmente

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências
        run: pip install requests

      - name: Buscar dados do Meta Ads
        env:
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
        run: python scripts/fetch_meta_ads.py

      - name: Commit e push
        run: |
          git config user.email "action@github.com"
          git config user.name "OGARCOM Bot"
          git add data/meta-ads.json
          git diff --staged --quiet || git commit -m "update: meta ads $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
