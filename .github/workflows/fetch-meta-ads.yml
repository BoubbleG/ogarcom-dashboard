name: Fetch Meta Ads Data
# Desabilitado: UTMify bloqueia IPs do GitHub Actions.
# Os dados são atualizados localmente pelo SINCRONIZAR.bat (scripts/fetch_utmify.js)

on:
  workflow_dispatch: # mantido apenas para rodar manualmente se necessário

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Dados atualizados via SINCRONIZAR.bat localmente."
